-- AWS HTTP Utilities Runtime Module
-- HTTP request and response helpers for AWS services
--
-- This module provides utilities for building HTTP requests,
-- parsing responses, and handling common AWS HTTP patterns.

-- =============================================================================
-- Status Codes
-- =============================================================================

{{
Check if an HTTP status code indicates success (2xx).
Returns true for status codes 200-299.
}}
aws.http.isSuccess : Nat -> Boolean
aws.http.isSuccess statusCode =
  Nat.gteq statusCode 200 && Nat.lt statusCode 300

{{
Check if an HTTP status code indicates a client error (4xx).
Returns true for status codes 400-499.
}}
aws.http.isClientError : Nat -> Boolean
aws.http.isClientError statusCode =
  Nat.gteq statusCode 400 && Nat.lt statusCode 500

{{
Check if an HTTP status code indicates a server error (5xx).
Returns true for status codes 500-599.
}}
aws.http.isServerError : Nat -> Boolean
aws.http.isServerError statusCode =
  Nat.gteq statusCode 500 && Nat.lt statusCode 600

{{
Check if an HTTP status code indicates an error (4xx or 5xx).
Returns true for status codes 400 and above.
}}
aws.http.isError : Nat -> Boolean
aws.http.isError statusCode =
  Nat.gteq statusCode 400

{{
Check if an HTTP status code indicates the request should be retried.

Retryable status codes:
- 429 Too Many Requests
- 500 Internal Server Error
- 502 Bad Gateway
- 503 Service Unavailable
- 504 Gateway Timeout
}}
aws.http.isRetryable : Nat -> Boolean
aws.http.isRetryable statusCode =
  match statusCode with
    429 -> true  -- Too Many Requests
    500 -> true  -- Internal Server Error
    502 -> true  -- Bad Gateway
    503 -> true  -- Service Unavailable
    504 -> true  -- Gateway Timeout
    _ -> false

-- =============================================================================
-- Header Helpers
-- =============================================================================

{{
Get a header value from a list of headers (case-insensitive).
Returns None if header not found.
}}
aws.http.getHeader : Text -> [(Text, Text)] -> Optional Text
aws.http.getHeader name headers =
  let
    lowerName = Text.toLowercase name
    find : [(Text, Text)] -> Optional Text
    find remaining =
      match remaining with
        [] -> None
        (k, v) +: rest ->
          if Text.eq (Text.toLowercase k) lowerName then Some v
          else find rest
    find headers

{{
Get a header value with a default if not found.
}}
aws.http.getHeaderOrDefault : Text -> Text -> [(Text, Text)] -> Text
aws.http.getHeaderOrDefault name defaultValue headers =
  match aws.http.getHeader name headers with
    Some value -> value
    None -> defaultValue

{{
Check if a header exists (case-insensitive).
}}
aws.http.hasHeader : Text -> [(Text, Text)] -> Boolean
aws.http.hasHeader name headers =
  match aws.http.getHeader name headers with
    Some _ -> true
    None -> false

{{
Add a header to a header list (does not check for duplicates).

Example: addHeader "X-Custom" "value" headers
}}
aws.http.addHeader : Text -> Text -> [(Text, Text)] -> [(Text, Text)]
aws.http.addHeader name value headers =
  (name, value) +: headers

{{
Set a header, replacing any existing header with the same name.

Example: setHeader "Content-Type" "application/json" headers
}}
aws.http.setHeader : Text -> Text -> [(Text, Text)] -> [(Text, Text)]
aws.http.setHeader name value headers =
  let
    lowerName = Text.toLowercase name
    filtered = List.filter (cases (k, _) -> not (Text.eq (Text.toLowercase k) lowerName)) headers
    (name, value) +: filtered

{{
Remove a header by name (case-insensitive).

Example: removeHeader "X-Custom" headers
}}
aws.http.removeHeader : Text -> [(Text, Text)] -> [(Text, Text)]
aws.http.removeHeader name headers =
  List.filter (cases (k, _) -> not (Text.eq (Text.toLowercase k) (Text.toLowercase name))) headers

{{
Merge two header lists, with the second list taking precedence.

Example: mergeHeaders baseHeaders customHeaders
}}
aws.http.mergeHeaders : [(Text, Text)] -> [(Text, Text)] -> [(Text, Text)]
aws.http.mergeHeaders base override =
  let
    overrideNames = List.map (cases (k, _) -> Text.toLowercase k) override
    filtered = List.filter (cases (k, _) -> not (List.contains (Text.toLowercase k) overrideNames)) base
    filtered ++ override

-- =============================================================================
-- Query String Helpers
-- =============================================================================

{{
Build a query string from key-value pairs.
Values are URL-encoded. Empty values are filtered out.
}}
aws.http.buildQueryString : [(Text, Text)] -> Text
aws.http.buildQueryString params =
  params
    |> List.filter (cases (_, v) -> not (Text.isEmpty v))
    |> List.map (cases (k, v) -> k ++ "=" ++ aws.http.urlEncode v)
    |> Text.join "&"

{{
Append a query string to a URL.
If URL already has a query string, appends with &.
}}
aws.http.appendQueryString : Text -> Text -> Text
aws.http.appendQueryString url queryString =
  if Text.isEmpty queryString then url
  else if Text.contains "?" url then url ++ "&" ++ queryString
  else url ++ "?" ++ queryString

{{
URL-encode a string for use in query parameters.

Encodes special characters as percent-encoded values.
}}
aws.http.urlEncode : Text -> Text
aws.http.urlEncode text =
  text
    |> Text.replaceAll "%" "%25"
    |> Text.replaceAll " " "%20"
    |> Text.replaceAll "!" "%21"
    |> Text.replaceAll "#" "%23"
    |> Text.replaceAll "$" "%24"
    |> Text.replaceAll "&" "%26"
    |> Text.replaceAll "'" "%27"
    |> Text.replaceAll "(" "%28"
    |> Text.replaceAll ")" "%29"
    |> Text.replaceAll "*" "%2A"
    |> Text.replaceAll "+" "%2B"
    |> Text.replaceAll "," "%2C"
    |> Text.replaceAll "/" "%2F"
    |> Text.replaceAll ":" "%3A"
    |> Text.replaceAll ";" "%3B"
    |> Text.replaceAll "=" "%3D"
    |> Text.replaceAll "?" "%3F"
    |> Text.replaceAll "@" "%40"
    |> Text.replaceAll "[" "%5B"
    |> Text.replaceAll "]" "%5D"

{{
URL-decode a percent-encoded string.
}}
aws.http.urlDecode : Text -> Text
aws.http.urlDecode text =
  text
    |> Text.replaceAll "%5D" "]"
    |> Text.replaceAll "%5B" "["
    |> Text.replaceAll "%40" "@"
    |> Text.replaceAll "%3F" "?"
    |> Text.replaceAll "%3D" "="
    |> Text.replaceAll "%3B" ";"
    |> Text.replaceAll "%3A" ":"
    |> Text.replaceAll "%2F" "/"
    |> Text.replaceAll "%2C" ","
    |> Text.replaceAll "%2B" "+"
    |> Text.replaceAll "%2A" "*"
    |> Text.replaceAll "%29" ")"
    |> Text.replaceAll "%28" "("
    |> Text.replaceAll "%27" "'"
    |> Text.replaceAll "%26" "&"
    |> Text.replaceAll "%24" "$"
    |> Text.replaceAll "%23" "#"
    |> Text.replaceAll "%21" "!"
    |> Text.replaceAll "%20" " "
    |> Text.replaceAll "%25" "%"

-- =============================================================================
-- URL Helpers
-- =============================================================================

{{
Build a full URL from scheme, host, path, and query string.
}}
aws.http.buildUrl : Text -> Text -> Text -> Text -> Text
aws.http.buildUrl scheme host path queryString =
  aws.http.appendQueryString (scheme ++ "://" ++ host ++ path) queryString

{{
Extract the host from a URL.
}}
aws.http.extractHost : Text -> Text
aws.http.extractHost url =
  let
    -- Remove scheme
    withoutScheme = 
      if Text.startsWith "https://" url then Text.drop 8 url
      else if Text.startsWith "http://" url then Text.drop 7 url
      else url
    -- Find end of host (first / or end of string)
    slashIdx = Text.indexOf "/" withoutScheme
    match slashIdx with
      Some idx -> Text.take idx withoutScheme
      None -> withoutScheme

{{
Extract the path from a URL (without query string).
}}
aws.http.extractPath : Text -> Text
aws.http.extractPath url =
  let
    -- Remove scheme
    withoutScheme = 
      if Text.startsWith "https://" url then Text.drop 8 url
      else if Text.startsWith "http://" url then Text.drop 7 url
      else url
    -- Find start of path
    slashIdx = Text.indexOf "/" withoutScheme
    match slashIdx with
      Some idx ->
        let
          pathAndQuery = Text.drop idx withoutScheme
          queryIdx = Text.indexOf "?" pathAndQuery
          match queryIdx with
            Some qIdx -> Text.take qIdx pathAndQuery
            None -> pathAndQuery
      None -> "/"

-- =============================================================================
-- Content Type Helpers
-- =============================================================================

{{
Common content type for XML.
}}
aws.http.contentTypeXml : Text
aws.http.contentTypeXml = "application/xml"

{{
Common content type for JSON.
}}
aws.http.contentTypeJson : Text
aws.http.contentTypeJson = "application/json"

{{
Common content type for form data.
}}
aws.http.contentTypeForm : Text
aws.http.contentTypeForm = "application/x-www-form-urlencoded"

{{
Common content type for binary data.
}}
aws.http.contentTypeOctetStream : Text
aws.http.contentTypeOctetStream = "application/octet-stream"

{{
Check if a content type is XML.
}}
aws.http.isXmlContentType : Text -> Boolean
aws.http.isXmlContentType contentType =
  Text.contains "xml" (Text.toLowercase contentType)

{{
Check if a content type is JSON.
}}
aws.http.isJsonContentType : Text -> Boolean
aws.http.isJsonContentType contentType =
  Text.contains "json" (Text.toLowercase contentType)

-- =============================================================================
-- AWS-Specific Headers
-- =============================================================================

{{
Standard AWS request ID header name.
}}
aws.http.requestIdHeader : Text
aws.http.requestIdHeader = "x-amz-request-id"

{{
AWS extended request ID header name (S3).
}}
aws.http.extendedRequestIdHeader : Text
aws.http.extendedRequestIdHeader = "x-amz-id-2"

{{
AWS date header name.
}}
aws.http.dateHeader : Text
aws.http.dateHeader = "x-amz-date"

{{
AWS security token header name.
}}
aws.http.securityTokenHeader : Text
aws.http.securityTokenHeader = "x-amz-security-token"

{{
AWS content SHA256 header name.
}}
aws.http.contentSha256Header : Text
aws.http.contentSha256Header = "x-amz-content-sha256"

{{
Extract the AWS request ID from response headers.
}}
aws.http.getRequestId : [(Text, Text)] -> Optional Text
aws.http.getRequestId headers =
  aws.http.getHeader aws.http.requestIdHeader headers

{{
Extract the AWS extended request ID from response headers.
}}
aws.http.getExtendedRequestId : [(Text, Text)] -> Optional Text
aws.http.getExtendedRequestId headers =
  aws.http.getHeader aws.http.extendedRequestIdHeader headers

-- =============================================================================
-- HTTP Request/Response Types (AWS Client Types)
-- =============================================================================

{{
HTTP Request type for building AWS requests.

This type is used internally by the AWS client. It uses simple types
(Text for method, URL; list for headers) for easy construction.

To execute requests, use aws.http.execute which bridges to @unison/http.
}}
type Http.Request = {
  method : Text,
  url : Text,
  headers : [(Text, Text)],
  body : Bytes
}

{{
HTTP Response type for AWS responses.

This type is used internally by the AWS client. It uses simple types
for easy handling in generated code.
}}
type Http.Response = {
  statusCode : Nat,
  headers : [(Text, Text)],
  body : Bytes
}

-- =============================================================================
-- Request Constructors
-- =============================================================================

{{
Create a GET request.
}}
Http.Request.get : Text -> [(Text, Text)] -> Http.Request
Http.Request.get url headers =
  Http.Request.Request "GET" url headers Bytes.empty

{{
Create a POST request.
}}
Http.Request.post : Text -> [(Text, Text)] -> Bytes -> Http.Request
Http.Request.post url headers body =
  Http.Request.Request "POST" url headers body

{{
Create a PUT request.
}}
Http.Request.put : Text -> [(Text, Text)] -> Bytes -> Http.Request
Http.Request.put url headers body =
  Http.Request.Request "PUT" url headers body

{{
Create a DELETE request.
}}
Http.Request.delete : Text -> [(Text, Text)] -> Http.Request
Http.Request.delete url headers =
  Http.Request.Request "DELETE" url headers Bytes.empty

{{
Create a HEAD request.
}}
Http.Request.head : Text -> [(Text, Text)] -> Http.Request
Http.Request.head url headers =
  Http.Request.Request "HEAD" url headers Bytes.empty

{{
Create a PATCH request.
}}
Http.Request.patch : Text -> [(Text, Text)] -> Bytes -> Http.Request
Http.Request.patch url headers body =
  Http.Request.Request "PATCH" url headers body

{{
Create an OPTIONS request.
}}
Http.Request.options : Text -> [(Text, Text)] -> Http.Request
Http.Request.options url headers =
  Http.Request.Request "OPTIONS" url headers Bytes.empty

-- =============================================================================
-- Type Bridge: AWS Types <-> @unison/http Types
-- =============================================================================
--
-- To make real HTTP requests, install the @unison/http library:
--
--   lib.install @unison/http/releases/8.0.0
--
-- Then uncomment the bridge functions in aws_http_bridge.u (provided separately)
-- or use the patterns documented in UNISON_HTTP_API_RESEARCH.md
--
-- Type mapping when using @unison/http:
--   Http.Request  { method: Text, url: Text, headers: list, body: Bytes }
--       ↓
--   HttpRequest   { method: Method, version: Version, uri: URI, headers: Headers, body: Body }
--
--   HttpResponse  { status: Status, version: Version, headers: Headers, body: Body }
--       ↓
--   Http.Response { statusCode: Nat, headers: list, body: Bytes }
--
-- =============================================================================

-- =============================================================================
-- HTTP Execution (Placeholder)
-- =============================================================================

{{
Execute an HTTP request (placeholder).

NOTE: This is a placeholder that returns an empty 200 OK response.
To make real HTTP requests, load aws_http_bridge.u after installing @unison/http.

Installation:
  lib.install @unison/http/releases/8.0.0

The bridge module will override executeRequest with real HTTP calls.
}}
aws.http.requestPlaceholder : Http.Request -> '{IO, Exception} Http.Response
aws.http.requestPlaceholder req _ =
  -- Placeholder: Return empty response
  Http.Response.Response 200 [] Bytes.empty

{{
Execute an HTTP request - main entry point for generated code.

This function is called by all generated S3 client operations.
By default, it uses the placeholder implementation.

To enable real HTTP requests:
1. Install @unison/http library
2. Load aws_http_bridge.u 
3. The bridge module will override this with real implementation

Usage in generated code:
  response = !(executeRequest (Http.Request.get url headers))
}}
executeRequest : Http.Request -> '{IO, Exception} Http.Response
executeRequest req =
  aws.http.requestPlaceholder req

-- =============================================================================
-- Response Helpers
-- =============================================================================

{{
Get a response header value by name.
}}
Http.Response.getHeader : Text -> Http.Response -> Optional Text
Http.Response.getHeader name resp =
  aws.http.getHeader name (Http.Response.headers resp)

{{
Decode bytes to UTF-8 text.
Requires Exception ability as the decode can fail.
}}
aws.http.bytesToText : Bytes ->{Exception} Text
aws.http.bytesToText bytes = fromUtf8 bytes

{{
Decode response body bytes to UTF-8 text.
Requires Exception ability as the decode can fail.
}}
Http.Response.bodyText : Http.Response ->{Exception} Text
Http.Response.bodyText resp = aws.http.bytesToText (Http.Response.body resp)

{{
Check if response indicates success (2xx status).
}}
Http.Response.isSuccess : Http.Response -> Boolean
Http.Response.isSuccess resp =
  aws.http.isSuccess (Http.Response.statusCode resp)

{{
Check if response indicates an error (4xx or 5xx status).
}}
Http.Response.isError : Http.Response -> Boolean
Http.Response.isError resp =
  aws.http.isError (Http.Response.statusCode resp)

{{
Ensure response is successful, raising exception if not.

Returns the response if successful (2xx), raises Failure otherwise.

Example:
  response = !aws.http.execute request
  !Http.Response.ensureSuccess response
  -- Now safe to process response body
}}
Http.Response.ensureSuccess : Http.Response ->{Exception} Http.Response
Http.Response.ensureSuccess resp =
  if Http.Response.isSuccess resp then resp
  else
    statusCode = Http.Response.statusCode resp
    bodyText = match catch do aws.http.bytesToText (Http.Response.body resp) with
      Left _ -> "(binary body)"
      Right t -> t
    Exception.raise (Failure (typeLink HttpError) ("HTTP error " ++ Nat.toText statusCode ++ ": " ++ bodyText) (Any resp))

{{
Type for HTTP errors.
}}
type HttpError = HttpError

{{
AWS HTTP Error type for structured error information.

Contains error details parsed from AWS XML error responses.
Includes all standard AWS error fields for comprehensive error handling.

Example AWS error XML:
  <Error>
    <Code>NoSuchBucket</Code>
    <Message>The specified bucket does not exist</Message>
    <RequestId>4442587FB7D0A2F9</RequestId>
    <HostId>...</HostId>
  </Error>
}}
structural type aws.http.Error = { 
  statusCode : Nat,    -- HTTP status code (e.g., 404, 403, 500)
  code : Text,         -- AWS error code (e.g., "NoSuchBucket", "AccessDenied")
  message : Text,      -- Human-readable error message
  requestId : Text,    -- AWS request ID for support/debugging
  hostId : Text        -- AWS host ID (S3 specific, for debugging)
}

{{
Convert an aws.http.Error to a Failure for exception handling.

The error message includes the AWS error code for easy identification.
}}
aws.http.Error.toFailure : aws.http.Error -> Failure
aws.http.Error.toFailure err =
  errorMessage = (aws.http.Error.code err) ++ ": " ++ (aws.http.Error.message err)
  Failure (typeLink aws.http.Error) errorMessage (Any err)

{{
Format an aws.http.Error as a detailed string for logging/display.

Includes all error fields in a readable format.
}}
aws.http.Error.toText : aws.http.Error -> Text
aws.http.Error.toText err =
  "AWS Error [" ++ Nat.toText (aws.http.Error.statusCode err) ++ "]\n" ++
  "  Code: " ++ (aws.http.Error.code err) ++ "\n" ++
  "  Message: " ++ (aws.http.Error.message err) ++ "\n" ++
  "  RequestId: " ++ (aws.http.Error.requestId err) ++ "\n" ++
  "  HostId: " ++ (aws.http.Error.hostId err)

{{
Extract text content between XML tags using simple text matching.
Used for parsing AWS error responses without depending on aws_xml.u.
}}
aws.http.extractXmlTag : Text -> Text -> Text
aws.http.extractXmlTag tagName xml =
  openTag = "<" ++ tagName ++ ">"
  closeTag = "</" ++ tagName ++ ">"
  match Text.indexOf openTag xml with
    None -> ""
    Some startIdx ->
      contentStart = startIdx + Text.size openTag
      restText = Text.drop contentStart xml
      match Text.indexOf closeTag restText with
        None -> ""
        Some endIdx -> Text.take endIdx restText

{{
Check if XML text contains an Error element.
}}
aws.http.isErrorXml : Text -> Boolean
aws.http.isErrorXml xml =
  match Text.indexOf "<Error>" xml with
    None -> false
    Some _ -> true

{{
Parse an AWS error from XML text.

Extracts all standard AWS error fields from XML error responses.
Returns an aws.http.Error record with parsed fields.
}}
aws.http.parseErrorXml : Nat -> Text -> aws.http.Error
aws.http.parseErrorXml statusCode xml =
  code = aws.http.extractXmlTag "Code" xml
  msg = aws.http.extractXmlTag "Message" xml
  reqId = aws.http.extractXmlTag "RequestId" xml
  hostId = aws.http.extractXmlTag "HostId" xml
  aws.http.Error.Error statusCode code msg reqId hostId

{{
Parse an AWS error from an HTTP response.

Attempts to parse the response body as AWS XML error format.
If not XML, creates a generic error with the HTTP status.
}}
aws.http.parseError : Http.Response -> aws.http.Error
aws.http.parseError resp =
  status = Http.Response.statusCode resp
  bodyText = fromUtf8 (Response.body resp)
  if aws.http.isErrorXml bodyText then
    aws.http.parseErrorXml status bodyText
  else
    aws.http.Error.Error status ("HTTP" ++ Nat.toText status) ("HTTP error: status " ++ Nat.toText status) "" ""

{{
Handle HTTP response errors.

Checks the status code and raises an Exception if it indicates an error.
For error responses, attempts to parse AWS XML error format to provide
detailed error information.

For 2xx status codes, returns Unit without error.
For 204 No Content, returns Unit (empty response is expected).
For 4xx/5xx status codes, raises an Exception with error details.
}}
handleHttpResponse : Http.Response ->{Exception} ()
handleHttpResponse resp =
  status = Http.Response.statusCode resp
  -- 2xx responses are successful
  if Nat.lt status 300 then ()
  else
    -- Error response - parse and raise exception
    errorInfo = aws.http.parseError resp
    Exception.raise (aws.http.Error.toFailure errorInfo)

-- =============================================================================
-- Error Code Checking
-- =============================================================================

{{
Check if an error matches a specific AWS error code.

Example:
  if aws.http.Error.isCode "NoSuchBucket" err then ...
}}
aws.http.Error.isCode : Text -> aws.http.Error -> Boolean
aws.http.Error.isCode expectedCode err =
  Text.eq (aws.http.Error.code err) expectedCode

{{
Check if the error indicates the resource was not found.

Matches common AWS "not found" error codes.
}}
aws.http.Error.isNotFound : aws.http.Error -> Boolean
aws.http.Error.isNotFound err =
  code = aws.http.Error.code err
  isNoSuchBucket = Text.eq code "NoSuchBucket"
  isNoSuchKey = Text.eq code "NoSuchKey"
  isNotFound = Text.eq code "NotFound"
  is404 = Text.eq code "404"
  isNoSuchBucket || isNoSuchKey || isNotFound || is404

{{
Check if the error indicates access was denied.

Matches common AWS access/permission error codes.
}}
aws.http.Error.isAccessDenied : aws.http.Error -> Boolean
aws.http.Error.isAccessDenied err =
  code = aws.http.Error.code err
  isAccessDenied = Text.eq code "AccessDenied"
  isForbidden = Text.eq code "Forbidden"
  is403 = Text.eq code "403"
  isAccessDenied || isForbidden || is403

{{
Check if the error indicates the request was throttled.

Matches AWS throttling error codes.
}}
aws.http.Error.isThrottled : aws.http.Error -> Boolean
aws.http.Error.isThrottled err =
  code = aws.http.Error.code err
  status = aws.http.Error.statusCode err
  isThrottling = Text.eq code "Throttling"
  isThrottlingException = Text.eq code "ThrottlingException"
  isSlowDown = Text.eq code "SlowDown"
  is429 = Nat.eq status 429
  isThrottling || isThrottlingException || isSlowDown || is429

{{
Check if the error is retryable.

Retryable errors include:
- 5xx server errors
- 429 Too Many Requests (throttling)
- Specific AWS error codes like "ServiceUnavailable"
}}
aws.http.Error.isRetryable : aws.http.Error -> Boolean
aws.http.Error.isRetryable err =
  status = aws.http.Error.statusCode err
  code = aws.http.Error.code err
  -- 5xx errors are generally retryable
  is5xx = Nat.gteq status 500 && Nat.lt status 600
  -- 429 is throttling
  is429 = Nat.eq status 429
  -- Specific retryable codes
  isServiceUnavailable = Text.eq code "ServiceUnavailable"
  isInternalError = Text.eq code "InternalError"
  isRequestTimeout = Text.eq code "RequestTimeout"
  isSlowDown = Text.eq code "SlowDown"
  isRetryableCode = isServiceUnavailable || isInternalError || isRequestTimeout || isSlowDown
  is5xx || is429 || isRetryableCode

-- =============================================================================
-- Try/Catch Helpers for AWS Errors
-- =============================================================================

{{
Try an operation and catch AWS errors, returning Either.

Left contains the aws.http.Error on failure.
Right contains the result on success.
}}
aws.http.try : '{IO, Exception} a -> '{IO} (Either aws.http.Error a)
aws.http.try action = do
  match Exception.catch action with
    Left failure -> 
      match Any.unsafeExtract (Failure.payload failure) with
        err -> Left err
    Right result -> Right result

{{
Try an operation and return Optional, discarding error details.
}}
aws.http.tryOptional : '{IO, Exception} a -> '{IO} (Optional a)
aws.http.tryOptional action = do
  match !(aws.http.try action) with
    Left _ -> None
    Right result -> Some result

{{
Check if a response indicates success (2xx status code).
}}
aws.http.isSuccessResponse : Http.Response -> Boolean
aws.http.isSuccessResponse resp =
  status = Http.Response.statusCode resp
  Nat.gteq status 200 && Nat.lt status 300

{{
Check if a response is empty (204 No Content or empty body).
}}
aws.http.isEmptyResponse : Http.Response -> Boolean
aws.http.isEmptyResponse resp =
  status = Http.Response.statusCode resp
  Nat.eq status 204 || Bytes.isEmpty (Response.body resp)

-- =============================================================================
-- Retry Logic
-- =============================================================================

{{
Retry configuration for AWS HTTP requests.

- maxRetries: Maximum number of retry attempts (default: 3)
- baseDelayMs: Base delay in milliseconds for exponential backoff (default: 100)
- maxDelayMs: Maximum delay cap in milliseconds (default: 20000)
- jitterFactor: Randomization factor 0.0-1.0 (default: 0.2)
}}
structural type aws.http.RetryConfig = {
  maxRetries : Nat,
  baseDelayMs : Nat,
  maxDelayMs : Nat
}

{{
Default retry configuration.

Uses AWS SDK defaults:
- 3 max retries
- 100ms base delay
- 20 second max delay
}}
aws.http.RetryConfig.default : aws.http.RetryConfig
aws.http.RetryConfig.default =
  aws.http.RetryConfig.RetryConfig 3 100 20000

{{
Create a retry config with custom max retries.
}}
aws.http.RetryConfig.withMaxRetries : Nat -> aws.http.RetryConfig
aws.http.RetryConfig.withMaxRetries n =
  aws.http.RetryConfig.RetryConfig n 100 20000

{{
Retry result type tracking attempts and final outcome.
}}
structural type aws.http.RetryResult a = {
  result : Either aws.http.Error a,
  attempts : Nat,
  totalDelayMs : Nat
}

{{
Calculate exponential backoff delay for a given attempt.

Uses formula: min(maxDelay, baseDelay * 2^attempt)
Returns delay in milliseconds.
}}
aws.http.calculateBackoff : aws.http.RetryConfig -> Nat -> Nat
aws.http.calculateBackoff config attempt =
  base = aws.http.RetryConfig.baseDelayMs config
  maxDelay = aws.http.RetryConfig.maxDelayMs config
  -- Calculate 2^attempt using repeated multiplication
  multiplier = Nat.pow 2 attempt
  delay = (Nat.*) base multiplier
  -- Cap at max delay
  if Nat.gt delay maxDelay then maxDelay else delay

{{
Parse Retry-After header value.

AWS may return Retry-After as:
- Number of seconds (e.g., "120")
- HTTP date (not supported, returns None)

Returns delay in milliseconds, or None if not parseable.
}}
aws.http.parseRetryAfter : Text -> Optional Nat
aws.http.parseRetryAfter headerValue =
  match Nat.fromText headerValue with
    Some seconds -> Some ((Nat.*) seconds 1000)
    None -> None

{{
Get retry delay from an HTTP response.

Checks Retry-After header first, falls back to calculated backoff.
}}
aws.http.getRetryDelay : aws.http.RetryConfig -> Http.Response -> Nat -> Nat
aws.http.getRetryDelay config resp attempt =
  retryAfterHeader = Response.getHeader "Retry-After" resp
  match retryAfterHeader with
    Some value -> 
      match aws.http.parseRetryAfter value with
        Some delayMs -> delayMs
        None -> aws.http.calculateBackoff config attempt
    None -> aws.http.calculateBackoff config attempt

{{
Sleep for the specified number of milliseconds.

Note: This is a placeholder that returns immediately.
Real delay requires @unison/io or similar library with Threads ability.
The delay parameter is tracked for retry logic even without actual sleeping.
}}
aws.http.sleepMs : Nat -> '{IO} ()
aws.http.sleepMs ms = do
  -- Placeholder - actual sleep requires additional IO primitives
  -- The ms parameter is intentionally unused until proper sleep is available
  _ = ms
  ()

{{
Execute an action with automatic retry on retryable errors.

Implements exponential backoff with optional Retry-After header support.

Returns RetryResult containing:
- Final result (success or last error)
- Number of attempts made
- Total delay time in milliseconds
}}
aws.http.withRetry : aws.http.RetryConfig -> '{IO, Exception} a -> '{IO} (aws.http.RetryResult a)
aws.http.withRetry config action =
  retryLoop attempt totalDelay =
    match Exception.catch action with
      Right result -> 
        aws.http.RetryResult.RetryResult (Right result) (attempt + 1) totalDelay
      Left failure ->
        -- Try to extract aws.http.Error from failure
        err = match Any.unsafeExtract (Failure.payload failure) with
          e -> e
        -- Check if we should retry
        shouldRetry = aws.http.Error.isRetryable err
        hasRetriesLeft = Nat.lt attempt (aws.http.RetryConfig.maxRetries config)
        canRetry = shouldRetry && hasRetriesLeft
        if canRetry then
          -- Calculate delay
          delayMs = aws.http.calculateBackoff config attempt
          -- Sleep then retry
          _ = !(aws.http.sleepMs delayMs)
          retryLoop (attempt + 1) (totalDelay + delayMs)
        else
          -- Return error result
          aws.http.RetryResult.RetryResult (Left err) (attempt + 1) totalDelay
  do retryLoop 0 0

{{
Execute an action with default retry configuration.

Convenience function using default retry settings.
}}
aws.http.withDefaultRetry : '{IO, Exception} a -> '{IO} (aws.http.RetryResult a)
aws.http.withDefaultRetry action =
  aws.http.withRetry aws.http.RetryConfig.default action

{{
Execute an action with retry, returning just the result.

Raises exception on final failure after all retries exhausted.
}}
aws.http.retryOrFail : aws.http.RetryConfig -> '{IO, Exception} a -> '{IO, Exception} a
aws.http.retryOrFail config action = do
  retryResult = !(aws.http.withRetry config action)
  match aws.http.RetryResult.result retryResult with
    Right a -> a
    Left err -> Exception.raise (aws.http.Error.toFailure err)

{{
Execute an action with default retry, returning just the result.

Convenience function that raises exception on final failure.
}}
aws.http.retryOrFailDefault : '{IO, Exception} a -> '{IO, Exception} a
aws.http.retryOrFailDefault action =
  aws.http.retryOrFail aws.http.RetryConfig.default action
